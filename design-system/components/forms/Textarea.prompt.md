Multi-line input for reversal reasons, activity notes and pasted legacy specifications.

```jsx
<Textarea rows={4} placeholder="Why is this entry being reversed?" />
<Textarea mono rows={10} />
```

A reversal reason is mandatory — pair with `Field` `required` and keep the typed text on a failed submit.
