The single-line text control. `numeric` switches to right-aligned tabular monospace for fixed-decimal amounts.

```jsx
<Input numeric suffix="USD" defaultValue="12,480.00" />
```

Never abbreviate or round the value shown in a numeric Input; the ledger uses fixed decimals.
