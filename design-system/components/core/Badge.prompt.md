A small non-interactive label for counts, identifiers and classifications (compatible / partial / manual, trial / applied).

```jsx
<Badge mono>BATCH-000148</Badge>
<Badge tone="warning">Partial</Badge>
```

For journal and job lifecycle states use `StatusPill` instead — it carries the state vocabulary and an icon.
